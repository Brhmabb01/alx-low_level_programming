#include "main.h"

/**
 * _isloer - check lowercase character
 * @c: the character to be checked
 * Return: 1 for lowercase charcter or 0 for anything else
 */

int _islower(int c)

{
	if (c >= 97 && c <=)
	{
	
	return (1);
	}
	return (0);

}
